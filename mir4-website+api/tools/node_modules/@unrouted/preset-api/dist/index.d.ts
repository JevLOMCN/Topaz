import * as _unrouted_core from '@unrouted/core';
import { CorsOptions } from 'cors';

interface PresetConfig {
    cors: boolean | CorsOptions;
}
declare const presetApi: (options?: Partial<PresetConfig> | undefined) => _unrouted_core.ResolvedPlugin;

export { PresetConfig, presetApi };
